 <p>Edit a budget</p>

  <form method="post" action="/budgets/<%= @budget.id %>/edit">
    <input id="hidden" type="hidden" name="_method" value="patch">
    You are editing the budget for '<%= Category.find(@budget.category_id).category_name %>'<br/>

    <label for="amount">Amount:</label>
    <input type="number" step="any" name="amount" value="<%= @budget.amount %>"><br/>

    <label for="rollover">Rollover?<label>
    <input type="radio" name="rollover" value=True <% "checked" if @budget.rollover %>>Yes
    <input type="radio" name="rollover" value=False <% "checked" if !@budget.rollover %>>No<br/>

    <input type="submit" value="Update">
  </form>
